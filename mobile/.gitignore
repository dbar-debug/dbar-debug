# Flutter/Dart
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
build/
*.iml
*.ipr
*.iws
.idea/

# Platform folders — generated locally via `flutter create .`
# (не комітимо, поки не буде потреби в кастомних нативних налаштуваннях)
/android/
/ios/
/macos/
/linux/
/windows/

# web/ генерується локально, але manifest/index беремо під git
# для брендування PWA (решта — іконки тощо — лишається локальною)
/web/*
!/web/manifest.json
!/web/index.html
!/web/README-pwa.md

# IDE
.vscode/
